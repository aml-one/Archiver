<UserControl x:Class="Archiver.UI.Views.JobsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="+ Add Job" Height="34" Width="110"
                    Background="{StaticResource AccentBrush}" Foreground="White"
                    BorderThickness="0"
                    Command="{Binding DataContext.AddNewJobCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
            <Button Content="🔄 Refresh" Height="34" Width="90"
                    Margin="8,0,0,0" BorderThickness="0"
                    Command="{Binding LoadJobsCommand}"/>
            <TextBlock VerticalAlignment="Center" Margin="20,0,0,0" Foreground="Gray">
                <Run Text="Total: "/><Run Text="{Binding Jobs.Count, Mode=OneWay}"/><Run Text=" jobs"/>
            </TextBlock>
        </StackPanel>

        <!-- Jobs List -->
        <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="6">
            <ListView ItemsSource="{Binding Jobs}" BorderThickness="0"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" Width="160">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse Width="8" Height="8" Margin="0,0,6,0" VerticalAlignment="Center">
                                            <Ellipse.Style>
                                                <Style TargetType="Ellipse">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Enabled}" Value="True">
                                                            <Setter Property="Fill" Value="#4CAF50"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Enabled}" Value="False">
                                                            <Setter Property="Fill" Value="#BDBDBD"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Ellipse.Style>
                                        </Ellipse>
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold"
                                                   VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Description" Width="180" DisplayMemberBinding="{Binding Description}"/>
                        <GridViewColumn Header="Source" Width="200" DisplayMemberBinding="{Binding SourcePath}"/>
                        <GridViewColumn Header="Schedule" Width="150" DisplayMemberBinding="{Binding ScheduleText}"/>
                        <GridViewColumn Header="Last Run" Width="130" DisplayMemberBinding="{Binding LastRun}"/>
                        <GridViewColumn Header="Status" Width="80" DisplayMemberBinding="{Binding LastStatus}"/>
                        <GridViewColumn Header="Copied" Width="70" DisplayMemberBinding="{Binding CopiedFiles}"/>
                        <GridViewColumn Header="Failed" Width="70" DisplayMemberBinding="{Binding FailedFiles}"/>
                    </GridView>
                </ListView.View>

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <EventSetter Event="MouseDoubleClick"
                                     Handler="JobItem_DoubleClick"/>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </Border>
    </Grid>
</UserControl>
